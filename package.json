{
  "name": "nfc-unlock",
  "version": "0.1.0",
  "description": "A simple Arduino to Nodejs serial communction program to be used as an authentication system for my nfc/rfid hand inplant to unlock an OS X computer.",
  "main": "server.js",
  "scripts": {
    "start": "forever -w --minUptime 30000 start -l nfc-unlock.log -a -o log/out.log -e log/err.log server.js"
  },
  "author": "Patric Lanhed",
  "license": "WTFPL",
  "dependencies": {
    "node-gyp": "^1.0.2",
    "serialport": "^2.0.2",
    "applescript": "^1.0.0"
  }
}
